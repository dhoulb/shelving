{
	"$schema": "https://json.schemastore.org/claude-code-settings.json",
	"hooks": {
		"Stop": [
			{
				"hooks": [
					{
						"type": "command",
						"command": "cd \"$CLAUDE_PROJECT_DIR\" && out=$(bun run fix 2>&1); ec=$?; if [ $ec -ne 0 ]; then jq -nc --arg r \"$out\" '{decision:\"block\", reason:(\"bun run fix failed — fix the remaining issues:\\n\" + $r)}'; fi",
						"statusMessage": "Running bun run fix"
					}
				]
			}
		]
	}
}
