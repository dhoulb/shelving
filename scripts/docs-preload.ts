// Loaded via `--preload` so the CSS-modules plugin is registered before any other module is loaded.
import { registerCssModulesPlugin } from "../modules/docs/util/cssModules.js";

registerCssModulesPlugin();
